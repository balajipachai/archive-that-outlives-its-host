{
  "name": "@archive/swarm-publisher",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "v13 bee-js adapter for the publisher: preflight, immutable batch checks, collection upload, and stable feed advancement. Owns the private-key boundary.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@archive/format": "workspace:*",
    "@ethersphere/bee-js": "13.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
